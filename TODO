1. context_latents加噪随sigma变化。

## 当前 Contextual Flow head 先跑一版后再验证/改进

先按当前代码跑完整训练；如果没有严重问题，不在这一版继续堆结构改动。训练结束后重点检查下面这些点：

1. Hidden condition 对 text editing / controllable generation 可能偏弱。
   - 当前 Qwen hidden condition 主要用于生成 AdaLN 的 shift / scale / gate，调制 flow head 每层 cross-attn 的 query 分支和 MLP 分支；最后 FinalLayer 也使用 y。
   - flow head 目前看不到完整文本 hidden 序列、编辑指令 token、可控条件 token；它只能假设 Qwen 已经把所有语义、编辑和控制信息压进当前 image token 的 hidden 里。
   - latent context 是每层 cross-attn 的 K/V memory，信息带宽更高；模型可能更依赖 visible / generated latent context，而不是充分响应 Qwen hidden condition。

2. 当前版本训练结束后做 condition / context 影响力 ablation。
   固定 x_t 和 context_latents：
   - 换 z_cond / z_uncond，看 velocity 差异有多大。
   - 固定 z，换 context_latents，看 velocity 差异有多大。
   - 看 CFG 下 v_cond - v_uncond 的范数是否足够大。
   - 对比以上差异，判断 hidden condition 是否被 latent context 抢主导。
   - 2026-07-01 初步 ablation 结论：
     - cfg=1 是 cond-only / no guidance amplification，不是无条件生成；当前公式下 cfg=0 constant 才是 image-uncond baseline。
     - cfg=0 vs cfg=1 图像对比显示 hidden condition 确实有语义控制力：cfg=1 明显更贴 ImageNet class，cfg=0 退化为更泛的 image prior。
     - z_cond - z_uncond RMS relative 约 20%-22%，说明 Qwen hidden condition 中 class/text 可见性差异是存在的。
     - v_cond - v_uncond RMS 约 0.05-0.06，相对 v_cond/v_uncond RMS 约 5%-6%；flow head 对 hidden condition 的响应有效但偏弱。
     - cfg=3/5 会放大该方向，但不稳定改善 latent MSE，说明 guidance direction 有语义但不够干净，过大可能带来 artifact 或偏移。
     - 结论：hidden condition 没有失效；更准确的问题是 condition path 带宽/影响力偏弱，下一版仍值得考虑更高带宽的 Qwen condition path，或增强 cond/uncond 对比训练。
     - 相关输出：output/selfless-flow-stage0-imagenet-full-from-qwen3base/manual_cfg_signal_nonema/ 和 output/selfless-flow-stage0-imagenet-full-from-qwen3base/manual_cfg0_uncond_nonema/。
   - 2026-07-01 CFG 外推 sweep 结论：
     - 修正后 CFG schedule 应该作用在 flow sampling time 上，而不是 image token fill progress；constant 比 linear 更适合当前验证肉眼观感。
     - cfg=5 constant 是当前较好的默认折中：自然图像感明显强于低 CFG，同时比更高 CFG 保留更多 diversity，artifact 也更少。
     - cfg=6-10 constant/linear 会继续增强 class prior 和自然图像感，但也更容易收缩到 class prototype，细节更“被 guidance 推着走”。
     - cfg=11+ constant 开始明显过 guided，主体和背景会变得奇怪；不建议作为默认，只适合找上限。
     - 不应依赖 guided full-sample MSE 判断 flow 拟合优劣；MSE 类指标应看 cfg=1 cond-only，cfg=5 主要用于视觉生成质量。
     - 相关输出：output/selfless-flow-stage0-imagenet-full-from-qwen3base/manual_cfg_extrapolate_to10_nonema/ 和 output/selfless-flow-stage0-imagenet-full-from-qwen3base/manual_cfg_constant_until_break_nonema/。

3. Qwen hidden 的幅度信息可能被洗掉。
   - _prepare_image_flow_condition 默认使用 RMS norm，再过 image_flow_condition_proj。
   - 这会让 condition 更稳定，但也会去掉 hidden norm / amplitude 中可能携带的置信度、可编辑性、难度或控制强度信息。
   - 训练后可以比较 image_flow_condition_norm=rms / none，或者记录 hidden RMS 与生成质量、CFG 差异的相关性。

4. 如果 hidden condition 确认偏弱，下一版考虑更高带宽的 Qwen condition path。
   - 保留当前 AdaLN-Zero 调制。
   - 额外加 Qwen hidden cross-attention，让 flow query 直接 attend 到 Qwen hidden tokens / relevant condition tokens。
   - condition K/V 不做 AdaLN 调制，保持 cache 友好；AdaLN 继续只调制 Q 和 MLP。
   - latent context cross-attn 继续负责 visible latent consistency，Qwen condition cross-attn 负责语义、编辑和控制。
